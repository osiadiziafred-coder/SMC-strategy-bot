# Core numerical / data stack (shared by the backtester and the ML robot)
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2

# Machine learning (ML/SMC brain)
scikit-learn==1.9.0
lightgbm==4.7.0
joblib==1.5.3

# MetaTrader 5 terminal API is Windows-only; skipped automatically on Linux/macOS
# so the training/backtest pipeline still installs and runs offline.
MetaTrader5>=5.0.45 ; platform_system == "Windows"

# Testing
pytest==8.3.3
