# Standalone requirements for the ML/SMC trading robot.
# (The repository root requirements.txt is a superset of this file.)
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.9.0
lightgbm==4.7.0
joblib==1.5.3

# Windows-only MetaTrader 5 terminal API. On Linux/macOS pip skips it and the
# robot falls back to the offline synthetic/CSV data providers for training and
# dry-run testing.
MetaTrader5>=5.0.45 ; platform_system == "Windows"
